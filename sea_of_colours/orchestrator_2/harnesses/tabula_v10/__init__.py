"""tabula_v10 — the COMPILER-FAITHFULNESS release (true fork of v9).

v9 completed its objectives: the comprehension agenda (attributed event digest,
two-case weapon-free redsign poker, engine-truth discoverer, the E1/E2/E4 engine
fixes) landed and v9 WON seed 69 three ways (mirror, vs-heuristic, vs-v8/v7).
v10 inherits all of that and attacks the failure class v9's audit exposed.

The audit's finding: every hallucination is in the EXECUTOR stage — it drops the
thinker's concrete cells and freelances (wrong drop cell, multi-drop on one unit,
zero-walk final-night drop, invented cell contents). The executor is not doing
creative work; it compiles a precise plan-token vocabulary into wire moves, on
the SAME game snapshot as the thinker (no new information). So v10's thesis is
the compiler pattern: creativity in the front (thinker), correctness in the back
(a deterministic packager).

What v10 owns (see :mod:`.harness` companion ``SEED69_FIXPLAN.md``, items R0–R6):
  * R0 — the thinker emits full per-token target geometry (drop / chain / probe /
    supersede cells) from the resolved agency registry, not just plan tokens.
  * R1 — a DETERMINISTIC Python packager compiles the recipe to wire moves;
    coordinate faithfulness + no-multi-drop hold by construction. The LLM mover
    is retained ONLY as the no-recipe fallback.
  * R3 — seat-differentiated target geometry (anti-crowd) so mirror seats fan out
    on public beacons instead of colliding.
  * R4/R5 — harvester economy gate + final-night live-harvester deploy rule.
  * :mod:`.digest`, :mod:`.prompt`, :mod:`.doctrine` — inherited from v9's
    FEEDING layer; :mod:`.rules` re-exports the frozen engine physics.

v6/v7/v8/v9 stay frozen. Shared-module changes v10 relies on are additive, so
the frozen agents are unaffected. The native-thinking-Sonnet bet (bigger model +
endpoint migration) is a separate later release (v11).
"""
