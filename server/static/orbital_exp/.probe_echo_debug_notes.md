# orbital_exp station UI — issues to fix next session

## Layout / positioning
- Station panels must sit **away from the map edge** — rhombus should be offset slightly toward the outside (away from the board) to leave breathing room for the animation strip between panel and board
- The `grid-area: map` / `.cc-main` grid fight needs a clean fix — current workaround is fragile

## Rhombus
- Should be **filled** (solid player-colour background), not just a border outline
- Tag text contrasts against filled background (dark text on bright fill)

## Pip / resource display
- Not rendering correctly — needs a full audit of what data is actually arriving in `_stationObs`
- "OBVS" mode (observer replay view) is triggering bad layout — need to guard or handle gracefully

## Mouseover / hover card
- Currently broken — card not appearing or mis-positioned
- Card should be a plain event log (per diagram 2): `· probe launched`, `▼ orblift dropped harvester`, `▲ orblift recovered harvester — empty · damaged`
- Need to verify `_lastActivity` is populated before the card tries to render

## Animations
- Shared canvas approach is right but needs a console-errors pass first — likely throwing early
- Comet trail (3 dots behind glyph) concept confirmed correct per diagrams
- Probe: small `■` flying horizontally toward board; orblift out: `▲`; return: `▲` (red if damaged)
- Y-stagger for simultaneous launches needs testing
- Animation strip lives between the station panel edge and the board edge — panel placement should leave that gap

## Plan for next session
1. Open browser console — fix all JS errors first
2. Confirm hooks are firing (add temporary console.log guards)
3. Fix rhombus fill + panel offset
4. Fix hover card positioning
5. Fix pip data path
6. Test animations on a real replay

## New bugs (2026-07-01, post-merge into server/static)

Moved to the canonical log: see `docs/OUTSTANDING_ISSUES.md`
- #5 — "LAUNCH DELAY" debug button leaks into the replay toolbar (move to Settings)
- #6 — replay flashes harvesters in their landed positions before the drop-off animation
